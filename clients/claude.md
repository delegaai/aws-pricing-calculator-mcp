# Claude

delega.ai for AWS Pricing Calculator is submitted separately to Anthropic's connector directory.

For manual remote MCP setup, use:

```text
https://calculator.delega.ai/mcp
```

If Claude asks for the service name, use:

```text
delega.ai for AWS Pricing Calculator
```

Start from the delega.ai client setup page:

```text
https://calculator.delega.ai/start/client
```

