# Docker MCP Toolkit

This repository documents the hosted delega.ai MCP server. Docker MCP Toolkit and Docker MCP Catalog can support remote hosted MCP servers, but catalog submission is managed through Docker's registry process.

Endpoint:

```text
https://calculator.delega.ai/mcp
```

Use this repository as the public documentation and support URL when preparing a Docker MCP Catalog submission.

